<form method="post" action="{{ path('app_typeabn_delete', {'idtypeA': typeabn.idtypeA}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeabn.idtypeA) }}">
    <button class="btn btn-primary">Delete</button>
</form>
